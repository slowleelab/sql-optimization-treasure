title: 函数索引优化 DATE 函数查询
category: optimizer
difficulty: 2
versions: ["8.0"]
tags: ["函数索引", "索引失效", "DATE函数", "8.0新特性"]
description: |
  查询使用 WHERE DATE(created_at) = '2024-01-15'，对索引列施加函数导致
  索引失效，退化为全表扫描。8.0 可直接创建函数索引 ((DATE(created_at)))
  使函数表达式也能走索引；也可改写为范围查询避免函数。
