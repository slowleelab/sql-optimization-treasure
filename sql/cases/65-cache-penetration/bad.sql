-- bad.sql: 直接查询不存在的用户 ID，缓存和数据库都 miss
-- 恶意请求使用 id=-1, id=99999999 等不存在的 ID 大量查询
-- 每次请求: 查缓存(miss) -> 查数据库(miss) -> 返回空
-- 数据库每次都执行一次主键查找，虽然单次很快，但 QPS 高时连接池耗尽
-- 攻击者用 1 万个不存在的 ID 每秒查 1000 次，数据库每秒执行 1000 次无效查询
SELECT * FROM t_user WHERE id = -1;
